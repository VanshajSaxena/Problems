> Problems that I solved during my programming journey

---

These problems mainly come from Leetcode. This is the typical structure of the files here:

``` java
// https://leetcode.com/problems/add-two-numbers/

/**
 * **Add Two Numbers**:
 *
 * You are given two **non-empty** linked lists representing two non-negative
 * integers. The digits are stored in **reverse order**, and each of their nodes
 * contains a single digit. Add the two numbers and return the sum as a linked
 * list.
 *
 * You may assume the two numbers do not contain any leading zero, except the
 * number 0 itself.
 */
public class AddTwoNumbers {

  /**
   * Naive Approach
   */
  public ListNode addTwoNumbersWithStringsI(ListNode l1, ListNode l2) {
  }

  /**
   * Better Approach
   */
  public ListNode addTwoNumbersWithStringsII(ListNode l1, ListNode l2) {
  }

  /**
   * Optimal Approach
   */
  public ListNode addTwoNumbersWithStringsIII(ListNode l1, ListNode l2) {
  }
}
```
